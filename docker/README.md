# Docker

Répertoire réservé aux composants qui seront plus simples à déployer en conteneurs séparés.

Le LXC Proxmox reste l'environnement de base. Docker ne sera introduit que lorsqu'un composant le justifie, afin d'éviter une complexité inutile sur le serveur.
